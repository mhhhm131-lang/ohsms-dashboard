
document.addEventListener("DOMContentLoaded", function () {
  const docCards = document.querySelectorAll("[data-doc]");
  const docContent = document.getElementById("docContent");

  function renderDoc(type) {
    if (!docContent) return;
    let html = "";
    if (type === "inspection") {
      html = `
        <h3>📝 نموذج فحص مكان العمل</h3>
        <ul>
          <li>بيانات الموقع (فرع – إدارة – قسم).</li>
          <li>تاريخ ووقت الفحص واسم الفاحص.</li>
          <li>قائمة نقاط الفحص (نظافة، مخارج طوارئ، معدات، تهوية...).</li>
          <li>الملاحظات والإجراءات التصحيحية.</li>
        </ul>
      `;
    } else if (type === "checklists") {
      html = `
        <p>يمكن ربط هذا النموذج لاحقاً بملف HTML خارجي يحتوي على مجموعة شيك لست كاملة.</p>
      `;
    } else if (type === "audit") {
      html = `
        <h3>📋 نموذج التفتيش / التدقيق</h3>
        <ul>
          <li>نطاق التدقيق.</li>
          <li>فريق التدقيق والتواريخ.</li>
          <li>بنود ISO 45001 محل المراجعة.</li>
          <li>نتائج المطابقة وعدم المطابقة.</li>
        </ul>
      `;
    } else if (type === "investigation") {
      html = `
        <h3>🛠 نموذج تحقيق في حادث</h3>
        <ul>
          <li>بيانات الحادث والمصابين.</li>
          <li>وصف ما حدث.</li>
          <li>الأسباب المباشرة وغير المباشرة.</li>
          <li>الأسباب الجذرية والإجراءات التصحيحية.</li>
        </ul>
      `;
    } else if (type === "incidentReport") {
      html = `
        <h3>📄 نموذج تقرير حادث</h3>
        <ul>
          <li>عنوان التقرير ورقم الحادث.</li>
          <li>تفاصيل الحادث والآثار.</li>
          <li>ملخص التحقيق والتوصيات.</li>
        </ul>
      `;
    } else if (type === "emergencyPlan") {
      html = `
        <h3>🔥 نموذج خطة الطوارئ</h3>
        <ul>
          <li>نوع الخطة (حريق، إخلاء، تسرب...).</li>
          <li>نطاق التطبيق.</li>
          <li>أدوار ومسؤوليات فرق الطوارئ.</li>
          <li>خطوات الاستجابة وقنوات الاتصال.</li>
        </ul>
      `;
    } else {
      html = `<h3>📂 عرض محتوى النموذج</h3><p>اضغط على نموذج من الأعلى.</p>`;
    }
    docContent.innerHTML = html;
  }

  docCards.forEach((card) => {
    card.addEventListener("click", () => {
      const t = card.getAttribute("data-doc");
      renderDoc(t);
    });
  });
});
